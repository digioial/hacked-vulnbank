import jwt
with open("/home/kali/tokens.txt", "w") as f:
    for numero in range(1, 41):
        payload = {
            "user_id": numero,
            "username": "nuovoutente",
            "is_admin": False,
            "iat": 1776708195
        }
        token = jwt.encode(payload, "secret123", algorithm="HS256")
        f.write(token + "\n")
